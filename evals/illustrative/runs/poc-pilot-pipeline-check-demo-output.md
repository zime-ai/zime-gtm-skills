9 of 9 rows in POC/Pilot stage — all in scope. Median POC duration (end−start, rows with both dates) = 92 days, computed from Acme(92), Globex(118), Zenith Solutions(91), NeoTech(61), Prism(92), API Gateway(92); drives check 6.

| Deal | Flags | Evidence | Suggested action |
|---|---|---|---|
| Quantum Systems | No exit criteria; No conversion date; No sponsor | `exit_criteria` = (empty); `poc_end_date` = (empty) and `conversion_date` = (empty); `sponsor` = (empty) | Get written success criteria + a named exec sponsor before spending more cycles, or kill it |
| Stellar Labs | No conversion date; Open-ended | `poc_end_date` = (empty) and `conversion_date` = (empty); `poc_start_date` = 2026-05-01, no end date, 108 days elapsed vs 92-day median | Set an end date now — already running 16 days past typical POC length with no defined exit |
| Zenith Solutions | No exit criteria | `exit_criteria` = (empty) | Get a written definition of what "won" looks like before next check-in |
| NeoTech Corp | Past its own end date | `poc_end_date` = 2026-07-10 (past, today 2026-08-17), `decision` = (empty) | Force a decision now — deal is 38 days stale with no recorded outcome |
| Prism Industries | No usage/milestone evidence | `poc_status` = live, `usage` = (empty) | Get usage data or explain why a live POC has none recorded |

5 of 9 deals in POC/Pilot flagged.
Most common flag: tie between "no written exit criteria" and "no conversion date" (2 deals each).
